
def search(term, num_results=10, lang="en"):

    #link_list =  getTextFromNasaWeb(term)
    #doc_name, documents = get_doc_list('txt')
    #lda_model_link = generateLDAmodel(doc_name, documents)

    new_list = []
    #for x in link_list:
     #   new_list.append(x)

    #for x in lda_model_link:
     #   new_list.append(x)

    return new_list
